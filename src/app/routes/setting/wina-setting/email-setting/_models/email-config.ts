/** store otp email configuration */
export interface EmailConfig {
  acceptCert: boolean;
  authenticationMethod: string;
  certificate: any;
  connectionSecurity: string;
  address: string;
  ignoreCert: boolean;
  name: string;
  port: string;
}
