/**
 * type of connection for connecting through different protocols
 */
export enum ConnectionType {
  RDP = 'rdp',
  TerminalService = 'terminalService',
  SSH = 'ssh',
  VNC = 'vnc',
  Telnet = 'telnet',
  Web = 'web',
}
